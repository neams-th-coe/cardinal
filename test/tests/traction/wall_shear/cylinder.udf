//
// nekRS User Defined File
//
#include "udf.hpp"

#ifdef __okl__

void velocityDirichletConditions(bcData *bc)
{
  if (bc->id==1)
  {
      bc->u = 1.0;
      bc->v = 0.0;
      bc->w = 0.0;
  }
}

#endif

/* UDF Functions */

void UDF_Setup(nrs_t *nrs)
{
  mesh_t* mesh = nrs->cds->mesh[0];
  int num_quadrature_points = mesh->Np * mesh->Nelements;

  for (int n = 0; n < num_quadrature_points; n++) {
    nrs->U[n + 0 * nrs->fieldOffset] = 0.001;
    nrs->U[n + 1 * nrs->fieldOffset] = 0.001;
    nrs->U[n + 2 * nrs->fieldOffset] = 0.001;

    nrs->cds->S[n + 0 * nrs->cds->fieldOffset[0]] = 0.0;
 }
}

void UDF_ExecuteStep(nrs_t *nrs, dfloat time, int tstep)
{
}
