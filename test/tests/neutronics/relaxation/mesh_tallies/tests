[Tests]
  [global_without_alignment]
    type = Exodiff
    input = openmc.i
    exodiff = openmc_out.e
    requirement = "The OpenMC wrapping shall perform transport with mesh tallies on a model with global tally "
                  "normalization without perfect alignment between the OpenMC mesh tallies and the mesh mirror."
  []
  [local_without_alignment]
    type = Exodiff
    input = openmc.i
    exodiff = out.e
    cli_args = "Problem/normalize_by_global_tally=false Outputs/file_base=out"
    requirement = "The OpenMC wrapping shall perform transport with cell tallies on a model with local tally "
                  "normalization without perfect alignment between the OpenMC mesh tallies and the mesh mirror."
  []
[]
