[Tests]
  [./incorrect_problem]
    type = RunException
    input = nek.i

    # nekRS can't use more processors than elements
    max_parallel = 12

    expect_err = "Postprocessor with name 'nek_avg_temp' can only be used with NekRSProblem!"
    requirement = "The system shall error if a Nek postprocessor is not paired with the correct problem."
  [../]
[]
