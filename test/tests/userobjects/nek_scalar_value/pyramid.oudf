void velocityDirichletConditions(bcData *bc)
{
  bc->u = 0.1;
  bc->v = 0.0;
  bc->w = 0.0;
}

void scalarDirichletConditions(bcData *bc)
{
  if (bc->id == 5)
    bc->s = bc->wrk[0 * bc->fieldOffset + 0];
  else if (bc->id == 6)
    bc->s = bc->wrk[0 * bc->fieldOffset + 1];
}

void scalarNeumannConditions(bcData *bc)
{
  bc->flux = 0.0;
}
