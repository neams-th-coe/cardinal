#ifdef __okl__

#include "pyramid.oudf"

#endif


void UDF_Setup()
{
}

void UDF_ExecuteStep(double time, int tstep)
{
  if (tstep > 0)
  {
    auto mesh = nrs->meshV;

    // set temperature value
    nrs->scalar->o_S.copyFrom(platform->app->bc->o_usrwrk, nrs->scalar->fieldOffset(), 0 * nrs->scalar->fieldOffset() /* write into slot 0 of o_S */);
  }
}
