[Tests]
  [./second_order_mesh]
    type = 'Exodiff'
    input = 'nek.i'
    exodiff = 'nek_out.e'
    cli_args = '--app nek --nekrs-setup pyramid'
    requirement = "NekMesh shall construct a second-order surface mesh from a list of boundary IDs. "
                  "The ordering of the nodes for each element shall also be based on the libMesh ordering."
  [../]
[]
